<span class="user-status">
  {{#if auth.signedIn}}
    <span class="signed-in">
      Signed In as {{auth.user.email}}
    </span>
    {{render sign_out}}
  {{else}}
    {{render sign_in}}
  {{/if}}
</span>